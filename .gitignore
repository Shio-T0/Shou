# Python / uv
__pycache__/
*.py[cod]
.venv/
shou/.venv/

# Runtime / local state (lives in ~/.config/shou, never the repo)
*.log
nohup.out
shou.conf
state

# Editor / OS cruft
.DS_Store
*.swp

# Claude Code workspace memory & transcripts — not part of the project
.claude/

# Unrelated workspace script that carries a private token
discord_ping.sh

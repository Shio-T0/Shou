# XcodeGen project definition for Shou Remote (iOS).
#
# This box is Linux, so the binary .xcodeproj can't be generated here. On a Mac:
#
#   brew install xcodegen          # one-off
#   cd ios && xcodegen generate    # writes Shou.xcodeproj from this file
#   open Shou.xcodeproj            # set your signing Team, then build/run
#
# Everything below mirrors android/app/build.gradle.kts (ids, version, min SDK).
name: Shou
options:
  bundleIdPrefix: io.github.shiot0
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: ""           # set your Apple Developer Team ID in Xcode
    CODE_SIGN_STYLE: Automatic
    SWIFT_VERSION: "5.9"
    ENABLE_USER_SCRIPT_SANDBOXING: YES
    GENERATE_INFOPLIST_FILE: NO

targets:
  # --- The app: a WKWebView shell + native superpowers ------------------------
  Shou:
    type: application
    platform: iOS
    sources:
      - path: Shou
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.shiot0.shou
        PRODUCT_NAME: Shou Remote
        INFOPLIST_FILE: Shou/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: Shou/Resources/Shou.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        TARGETED_DEVICE_FAMILY: "1,2"   # iPhone + iPad
        SUPPORTS_MACCATALYST: NO
    dependencies:
      - target: ShouWidget
        embed: true

  # --- The widget + Control Center extension ---------------------------------
  ShouWidget:
    type: app-extension
    platform: iOS
    sources:
      - path: ShouWidget
      # Shared core compiled into both targets (no framework needed for this size).
      - path: Shou/Core
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.shiot0.shou.widget
        PRODUCT_NAME: ShouWidget
        INFOPLIST_FILE: ShouWidget/Info.plist
        CODE_SIGN_ENTITLEMENTS: ShouWidget/ShouWidget.entitlements
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
